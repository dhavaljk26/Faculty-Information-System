<div class="interest_area">
  <div>
    <%= content_tag :li, "#{interest_area.name}"%> <%= link_to "Delete", [@faculty, interest_area], method: :delete  if logged_in? and (current_faculty.admin or current_faculty.id == @faculty.id)  %>
  </div>
</div>